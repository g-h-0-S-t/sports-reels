{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "installCommand": "apt-get update && apt-get install -y python3 python3-pip && pip3 install --no-cache-dir -r requirements.txt && python3 -m pip --version && echo 'Python dependencies installed successfully'",
        "buildCommand": "npm run build || { echo 'Build failed'; exit 1; }"
      }
    }
  ],
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ],
  "env": {
    "PYTHON_VERSION": "3.8"
  }
}
