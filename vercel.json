{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "installCommand": "apt-get update && apt-get install -y python3 python3-pip || { echo 'Failed to install Python'; exit 1; } && pip3 install -r requirements.txt || { echo 'Failed to install Python dependencies'; exit 1; }",
        "buildCommand": "npm run build || { echo 'Build failed'; exit 1; }"
      }
    }
  ],
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ],
  "env": {
    "PYTHON_VERSION": "3.8"
  }
}
