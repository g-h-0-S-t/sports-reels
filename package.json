{
  "name": "sports-reels",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pip3 install -r requirements.txt && (node scripts/check-videos.js || echo 'Skipping check-videos') && next dev",
    "build": "pip3 install -r requirements.txt && (node scripts/check-videos.js || echo 'Skipping check-videos') && next build",
    "start": "next start",
    "vercel-build": "next build",
    "generate-videos": "node scripts/check-videos.js || echo 'Skipping check-videos'",
    "clean-videos": "rm -rf /tmp/videos/*.mp4 public/videos/* || del /q \\tmp\\videos\\*.mp4 public\\videos\\*",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx",
    "install-python-deps": "if [ \"$OS\" != \"Windows_NT\" ]; then chmod -R u+rw .; else icacls . /grant %username%:F /T; fi && pip install -r requirements.txt || pip3 install -r requirements.txt",
    "postinstall": "echo 'Python dependencies installed automatically on npm run dev/build/start. Manual install: pip install -r requirements.txt'"
  },
  "dependencies": {
    "next": "^15.0.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-next": "^15.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}